apiVersion: v1
kind: Service
metadata:
  name: {{ include "deallus-orchestrator.fullname" . }}
  labels:
    {{- include "deallus-orchestrator.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "deallus-orchestrator.selectorLabels" . | nindent 4 }}
